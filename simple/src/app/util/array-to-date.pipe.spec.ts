import { ArrayToDatePipe } from './array-to-date.pipe';

describe('ArrayToDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ArrayToDatePipe();
    expect(pipe).toBeTruthy();
  });
});
